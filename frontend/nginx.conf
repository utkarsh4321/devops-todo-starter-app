
        server{
    listen 80;          # Listen for IPv4
    listen [::]:80;     # Listen for IPv6
    server_name localhost;

    root /mywebsite;
    index index.html;


    location / {
        try_files $uri /index.html;
    }
    }

